import { makeStyles } from "@material-ui/core/styles";

const styles = makeStyles({
  horizontalLine: {
    width: "100%",
    borderBottom: `2px solid rgba(160,160,160,0.2)`,
  },
});

export default styles;
