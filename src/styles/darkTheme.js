import { grey } from "@material-ui/core/colors";

const material = {
  palette: {
    type: "dark",
    border: grey[800],
    primary: {
      main: "#333333",
    },
    background: {
      default: "#212121",
    },
  },
};

export default material;
