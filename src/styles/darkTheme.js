import { grey } from "@material-ui/core/colors";

const darkTheme = {
  palette: {
    type: "dark",
    border: grey[800],
    primary: {
      main: "#333333",
    },
    background: {
      default: "#212121",
    },
  },
};

export default darkTheme;
